{
    "name": "arc-portal-contracts",
    "version": "1.0.0",
    "description": "ARC Portal smart contracts",
    "scripts": {
        "compile": "npx hardhat compile",
        "test": "npx hardhat test",
        "clean": "npx hardhat clean",
        "export-artifacts": "node scripts/export-artifacts.js",
        "verify": "npx hardhat run scripts/verify-contract.ts --network arcTestnet"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "@openzeppelin/contracts": "^5.0.0",
        "hardhat": "^2.22.0"
    },
    "dependencies": {
        "dotenv": "^17.3.1"
    }
}
