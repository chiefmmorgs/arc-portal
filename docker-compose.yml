services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: arcuser
      POSTGRES_PASSWORD: arcpass
      POSTGRES_DB: arc_portal
    ports:
      - "5555:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata:
